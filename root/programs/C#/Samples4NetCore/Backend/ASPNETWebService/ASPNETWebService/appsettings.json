{
  // 簡易ログの設定
  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  },
  // Here is where you can supply custom configuration settings, Since it is is JSON,
  // everything is represented as key: value pairs. Name of section is your choice.
  "AppConfiguration": {
    "XXXX": "xxxx",
    "YYYY": "yyyy"
  }
  // 以下は*.condifから移行されたsection

}
