<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
	SELECT * FROM REGION<WHERE>WHERE<IF name="P">RegionID IS NOT NULL<ELSE>RegionID IS NULL</ELSE></IF></WHERE>
	<PARAM>
		P, int32, 1<DIV/>
	</PARAM>
	-- IF-ELSEタグ指定（name属性に無効な値の指定でエラー）。
</ROOT>
