<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
  SELECT * FROM REGION<WHERE>
    WHERE<SUB>RegionID IN(SELECT Territories.RegionID FROM Territories)</SUB>
  </WHERE>
  <PARAM>
    og, int32, 1<DIV/>
  </PARAM>
  -- SUBタグ指定（無効な値の指定でエラー）。
</ROOT>