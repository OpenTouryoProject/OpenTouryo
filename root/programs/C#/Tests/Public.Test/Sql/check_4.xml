<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
  SELECT * FROM REGION<WHERE>
    WHERE<IF name="P">
      <ELSE name="Q">
      </ELSE>
    </IF>
    <ELSE></ELSE>
  </WHERE>
  <PARAM>
    Q,boolean,true<DIV/>
  </PARAM>
  -- IF-ELSEタグ指定（name属性に無効な値の指定でエラー）。
</ROOT>
