<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
	SELECT * FROM ADMINISTRATOR.EMPLOYEE
	WHERE
	EMPNO IN (SELECT EMPNO FROM ADMINISTRATOR.EMPLOYEE)
	AND SEX IN (@PLIST1, @PLIST2)
	ORDER BY<VAL name="COLUMN"/><VAL name="SEQUENCE"/>
	<PARAM>
		PLIST1, Char, M <DIV/>
		PLIST2, Char, F <DIV/>
		COLUMN,EMPNO<DIV/>
		SEQUENCE,DESC<DIV/>
	</PARAM>
	-- パラメタ設定済み（sql版をそのまま、dpq版に移行したものなので静的）
</ROOT>
