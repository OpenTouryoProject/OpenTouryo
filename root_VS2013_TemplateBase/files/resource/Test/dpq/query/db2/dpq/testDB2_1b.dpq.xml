<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
	SELECT * FROM ADMINISTRATOR.EMPLOYEE
	<WHERE>
		WHERE
		<SUB name="SUB1">
			EMPNO IN(SELECT EMPNO FROM ADMINISTRATOR.EMPLOYEE
			<WHERE>
				WHERE
				<SUB name="SUB2">
					WORKDEPT IN(SELECT DEPTNO FROM ADMINISTRATOR.DEPARTMENT
					<WHERE>
						WHERE
						<IF name="ISNOTNULL1">MGRNO IS NOT NULL<ELSE>MGRNO IS NULL</ELSE></IF>
						<LIST>AND ADMRDEPT IN(@ADMRDEPT1)</LIST>
					</WHERE>)
				</SUB>
				<IF name="ISNOTNULL2">AND WORKDEPT IS NOT NULL<ELSE>AND WORKDEPT IS NULL</ELSE></IF>
				<LIST>AND SEX IN(@SEX1)</LIST>
			</WHERE>)
		</SUB>
		<SUB name="SUB3">
			AND EMPNO IN(SELECT EMPNO FROM ADMINISTRATOR.EMPLOYEE
			<WHERE>
				WHERE
				<SUB name="SUB4">
					WORKDEPT IN(SELECT DEPTNO FROM ADMINISTRATOR.DEPARTMENT
					<WHERE>
						WHERE
						<IF name="ISNOTNULL3">MGRNO IS NOT NULL<ELSE>MGRNO IS NULL</ELSE></IF>
						<LIST>AND ADMRDEPT IN(@ADMRDEPT2)</LIST>
					</WHERE>)
				</SUB>
				<IF name="ISNOTNULL4">AND WORKDEPT IS NOT NULL<ELSE>AND WORKDEPT IS NULL</ELSE></IF>
				<LIST>AND SEX IN(@SEX2)</LIST>
			</WHERE>)
		</SUB>
		<IF name="ISNOTNULL">AND WORKDEPT IS NOT NULL<ELSE>AND WORKDEPT IS NULL</ELSE></IF>
		<LIST>AND SEX IN(@SEX)</LIST>
	</WHERE>
	ORDER BY<VAL name="COLUMN"/><VAL name="SEQUENCE"/>
	<PARAM>
		SUB2, Boolean, true<DIV/>
		SUB3, Boolean, true<DIV/>
		SUB4, Boolean, true<DIV/>
		ISNOTNULL1, Boolean, true<DIV/>
		ISNOTNULL2, Boolean, true<DIV/>
		ISNOTNULL3, Boolean, true<DIV/>
		ISNOTNULL4, Boolean, true<DIV/>
		ISNOTNULL, Boolean, true<DIV/>
		ADMRDEPT1, String, A00, D01, E01<DIV/>
		ADMRDEPT2, String, A00, D01, E01<DIV/>
		SEX1, Char, M, F<DIV/>
		SEX2, Char, M, F<DIV/>
		SEX, Char, M, F<DIV/>
		COLUMN, EMPNO<DIV/>
		SEQUENCE, DESC<DIV/>
	</PARAM>
	-- テストケース1b（SUB1を削ってみた。ANDが上手く消えるかどうかと、設定周りに使い難さが無いか確認）
</ROOT>
