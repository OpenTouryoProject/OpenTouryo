<?xml version="1.0" encoding="UTF-8" ?>
<ROOT>
	SELECT * FROM REGION<WHERE>WHERE<IF>'@P' = '@P' and regionid = @P</IF></WHERE>
	<PARAM>
		P, int32, 1
	</PARAM>
	-- パラメタ文字の検出ロジックの確認（''で囲われた文字は、パラメタとして認識しない）。
</ROOT>
